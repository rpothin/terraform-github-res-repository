# Basic Example

This example demonstrates the minimum configuration required to create a private GitHub repository using this module.

The repository is created with private visibility (secure by default) and an initial README commit.
